# The active product uses host-wallet collection and the Vercel cashback API.
# The shared-wallet functions under supabase/functions are retained only as
# historical source and intentionally are not declared here. Supabase's Git
# integration deploys declared functions on pushes; declaring these legacy
# functions would bundle @nimiq/core server-side (~31 MB) and exceed the Edge
# Function upload limit. Existing deployed versions are not deleted by this.
